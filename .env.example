# Copy this file to .env and fill in real keys.
# .env is gitignored — your real keys never get committed.
#
# You only need a key for whichever model provider(s) your agents are using.
# Unused providers can be left blank.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
